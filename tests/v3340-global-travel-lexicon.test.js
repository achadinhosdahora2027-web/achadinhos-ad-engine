'use strict';
const assert=require('assert');const crypto=require('crypto');const fs=require('fs');const path=require('path');
const ROOT=path.resolve(__dirname,'..');const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8');const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(path.join(ROOT,p))).digest('hex');
const SQL=read('supabase/migrations/supabase_v3340_global_travel_lexicon.sql');const EDGE=read('supabase/functions/nexus-global-travel-v3340/index.ts');const COPY=read('supabase/functions/nexus-copywriter-v3200/index.ts');
assert.match(SQL,/create table if not exists public\.nexus_v3340_travel_lexicon/i);assert.doesNotMatch(SQL,/create\s+unlogged\s+table[\s\S]*nexus_v3340_travel_lexicon/i);assert.match(SQL,/embedding\s+public\.halfvec\(384\)\s+not null/i);assert.match(SQL,/using hnsw\(embedding public\.halfvec_cosine_ops\)/i);assert.match(SQL,/r\.embedding<=>v_query/);assert.doesNotMatch(SQL,/r\.embedding<->v_query/);assert.match(SQL,/cosine_threshold[^\n]*0\.8000/i);
for(const [country,count] of [['US',26],['CA',15],['ES',14],['FR',12],['IT',13],['GB',12]])assert.match(SQL,new RegExp(`'${country}',${count}`));
for(const city of ['New York City','Los Angeles','Washington D.C.','Honolulu','Toronto','Montreal','Quebec City','Madrid','Palma de Mallorca','San Sebastián','Paris','Marseille','Saint-Étienne','Roma','Milano','Venezia','London','Edinburgh','Cambridge','York'])assert.ok(SQL.includes(city),city);
assert.match(SQL,/operator_reference_not_independently_verified/g);assert.match(SQL,/current_setting\('nexus\.v3340_travel_embeddings_json'/i);assert.match(SQL,/jsonb_array_length\(v_payload->'rows'\)<>92/i);assert.match(SQL,/set local statement_timeout='2000ms'/i);assert.match(SQL,/set local lock_timeout='1000ms'/i);assert.match(SQL,/exception when others/i);assert.match(SQL,/Sintonizado em Análise/i);
assert.match(SQL,/create or replace function public\.nexus_v3340_resolve_travel/i);assert.match(SQL,/grant execute on function public\.nexus_v3340_resolve_travel[\s\S]*to service_role/i);assert.match(SQL,/case lower\(e\.key\) when 'cf-ipcountry' then 0 else 1 end/i);assert.match(SQL,/destination_absent_from_intent/i);assert.match(SQL,/travel_intent_not_explicit/i);assert.match(SQL,/locale_not_supported_for_destination/i);
assert.match(SQL,/operator_epc_328_20_matches_current_catalog/);assert.match(SQL,/booking_uk_epc_7d_current/);assert.match(SQL,/current_catalog_epc_7d',9\.35/);assert.match(SQL,/current_catalog_epc_3m',11\.15/);assert.match(SQL,/booking_global',false/g);
assert.doesNotMatch(SQL,/(?:insert\s+into|update|delete\s+from)\s+public\.ads\b/i);assert.match(SQL,/jobid=60[\s\S]*schedule='10 seconds'/i);assert.doesNotMatch(SQL,/cron\.schedule|cron\.unschedule/i);assert.match(SQL,/jobid in\(15,16,64\) and active/i);assert.match(SQL,/v_persistence<>'u'/i);assert.match(SQL,/v_persistence<>'p'/i);
assert.match(EDGE,/NEXUS_V3340_EDGE_SECRET/);assert.match(EDGE,/Supabase\.ai\.Session\(MODEL\)/);assert.match(EDGE,/MODEL = "gte-small"/);assert.match(EDGE,/DIMS = 384/);assert.match(EDGE,/rpc\/nexus_v3340_resolve_travel/);assert.match(EDGE,/request\.headers\.get\("cf-ipcountry"\)/);assert.match(EDGE,/request\.headers\.get\("x-vercel-ip-country"\)/);assert.match(EDGE,/redirect_performed: false/);assert.match(EDGE,/click_recorded: false/);assert.match(EDGE,/shortener_modified: false/);assert.doesNotMatch(EDGE,/Response\.redirect|location\s*:/i);
assert.match(COPY,/V3350_POLICY_VERSION/);assert.strictEqual(sha('api/ads/go.js'),'e77aab2895e8a194542866bf7c9e997a0fe37138638ce57125c8536009827716');assert.strictEqual(sha('edge/jetstream/compose.ts'),'d99ce7b5ca412102659f44d6d58f8f84f1b111f0f08865f208f13fd12cc730d0');
console.log('v3340 multilingual travel lexicon tests: PASS');
